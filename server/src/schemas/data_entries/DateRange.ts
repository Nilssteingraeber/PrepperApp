export class DateEntry {

    startDate: Date;
    endDate: Date;

    constructor(
        start: Date, end: Date
    ) {

        this.startDate = start;
        this.endDate = end;
    }
}

export default DateEntry